plugins {
  id 'java'
  // buildSrc supplies task classes, not Gradle plugins or plugin descriptors.
  id 'groovy'
}

repositories {
  mavenCentral()
}

dependencies {
  implementation gradleApi()
  implementation 'org.openjdk.asmtools:asmtools-core:7.0.b10-ea'
}
