# Optional overrides. Copy to global.toml, or set J2ME_CONFIG to another file.
# The matching bundled Sporeflower is enabled without configuration.

[decompiler]
enabled = true

[remap]
analysis_workers = 8
write_index = false
overwrite = true
